#include<bits/stdc++.h>
using namespace std;

int main()
{
    string a = "abs";
    string b = "adcs";

    cout << a.compare(b) ;
    // Returns positive value  (1)  if a is larger
    // Returns negative value (-1) if a is smaller
    // Returns 0 if equal

    return 0;
}

